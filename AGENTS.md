# AGENTS

저는 `옥진수학`에서 기획 및 관리자, 선생님의 역할을 하고 있습니다.

당신은 엄청난 수석 웹앱 개발자입니다.

## 기본 소통 규칙

1. 코드 관련 설명은 초~중학생 눈높이로 쉽게 설명합니다.
2. 제가 설명한 기획 의도를 먼저 확인한 뒤 코드를 작성합니다.

## 새 스레드 시작 규칙

새 스레드에서 이 저장소(`v1`)를 다룰 때는, 코드 작업 전에 아래 파일을 먼저 읽고 현재 기준 상태를 요약한 뒤 시작합니다.

- `docs/project-status-latest.md`

## 배포/상태 기록 규칙

- 배포(특히 프로덕션) 또는 큰 패치가 끝나면 `docs/project-status-latest.md`의 기준 시각과 운영 배포 항목을 즉시 갱신합니다.
- 운영 배포 이력에는 최소한 배포 시각, 운영 주소, 배포 식별자(있다면)를 함께 남깁니다.

## Google 401 복구 패치 기본 방향 (2026-03-24)

- Google Calendar API에서 `401`이 발생하면, 사용자를 가능한 한 자동으로 `구글 권한 다시 연결` 흐름으로 안내합니다.
- 자동 안내는 `next` 복귀 경로를 유지해, 재인증 후 원래 보던 페이지로 돌아오게 합니다.
- 동일한 `401`은 짧은 시간 동안 중복 알림/로그를 억제해 콘솔 도배와 UX 피로를 줄입니다.
- 동기화 엔진 내부 상태 패치(`synced/error`)가 다시 동기화 재호출을 만들지 않도록 루프 방지 가드를 유지합니다.

## 회차 동기화 운영 정책 (2026-03-24)

- 회차/Meet 생성 소유자는 항상 `담당 선생님 이메일` 기준입니다.
- 관리자 계정이 담당 선생님과 이메일이 다를 때는 직접 생성하지 않고 `pending` 재동기화 요청으로 처리합니다.
- `a/tmain`, `t/tmain` 학생 리스트 상단에 `본인 학생 회차 동기화` 버튼을 두고, 현재 화면의 담당 학생 묶음을 한 번에 재동기화 요청할 수 있습니다.
- 잘못된 소유자 계정에서 회차 정보가 변경되면 상태를 `pending`으로 내려, 담당 선생님 계정 로그인 후 정상 재생성을 유도합니다.
